#include <stdio.h>
#include <stdlib.h>
#include "cc.h"

/* Encapsulate a threading framework
 * (note): The idea is to construct each module (assuming modular independancy is ok) on a seperate thread if enough threads exist.
 */

 // function checking lexical analysis workloads via file stat


